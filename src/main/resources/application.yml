spring:
    datasource:
        url: jdbc:mysql://localhost:3306/gestion-tickets
        username: root
        password: root
    jpa:
        show_sql: true
        hibernate:
            ddl-auto: update
        properties:
            dialect: org.hibernate.dialect.MySQL5Dialect
    thymeleaf:
        cache: false
logging:
    level:
        org:
            springframework:
                security: DEBUG
